version: "3.7"

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: 'rabbitmq'
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - ~/.docker-conf/rabbitmq/data/:/var/lib/rabbitmq/
      - ~/.docker-conf/rabbitmq/log/:/var/log/rabbitmq
    networks:
      - rabbitmq_go_net

  web:
    build: ./main_backend
    volumes:
      - ./main_backend:/app
      - ./nft-generator/output:/app/static/img-tracking/data/nft
    networks:
      - rabbitmq_go_net
      - nginx
    depends_on:
      - rabbitmq

  nft_generator:
    build: ./nft-generator
    volumes:
      - ./nft-generator:/app
      - ./main_backend/static/img-tracking/data/images:/app/main_backend_images
    networks:
      - rabbitmq_go_net
    depends_on:
      - web
      - rabbitmq

  nginx:
    build: ./nginx
    ports:
      - "443:443"
    volumes:
      - ./main_backend/staticfiles:/app/static
    depends_on:
      - web
    networks:
      - nginx


networks:
  rabbitmq_go_net:
    driver: bridge
  nginx:
    driver: bridge
